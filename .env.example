# SEC requires a real contact in the User-Agent (fair-access policy).
SEC_USER_AGENT="Joshua Lacroix Joshua.lacroix8b@gmail.com"

# Phase 2+ (prices, news, earnings calendar)
FINNHUB_API_KEY=

# Phase 4 (macro)
FRED_API_KEY=

# Phase 3 (alerts, e.g. Gmail app password)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL_TO=

# Phase 5 only, optional — site must run without it
ANTHROPIC_API_KEY=

TZ="America/Chicago"
